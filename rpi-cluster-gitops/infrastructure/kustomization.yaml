apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - longhorn
  # monitoring/ requires the Prometheus Operator CRDs, which aren't installed
  # yet (see rpi-cluster-gitops/README.md's "Known gaps") - re-add once that's
  # in place, otherwise its PrometheusRule blocks this whole Kustomization,
  # including Longhorn, since Flux applies a Kustomization's resources together.
  # - monitoring
