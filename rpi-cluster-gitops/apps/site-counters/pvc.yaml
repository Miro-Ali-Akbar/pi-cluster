apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: site-counters-data
  namespace: site-counters
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fast-ssd
  resources:
    requests:
      storage: 100Mi
