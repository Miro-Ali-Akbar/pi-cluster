apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: site-counters-data
  namespace: site-counters
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: seaweedfs-storage
  resources:
    requests:
      storage: 100Mi
